# TODO - Automation EDA (frontend-only)

- [ ] Verify and remove unused/non-required auth middleware and (auth) routes/folders.
- [ ] Remove author GitHub/X links from components/layout.
- [ ] Remove user account (avatar/dropdown/user menu) from navbar/header (or sidebar footer/header) cleanly.
- [ ] Replace `src/navigation/sidebar/sidebar-items.ts` with the required Automation EDA navigation structure (incl. collapsibles + NEW badge).
- [ ] Add “Dataset Information” card section at bottom of sidebar with placeholder/static values.
- [ ] Create placeholder pages for every sidebar route under `src/app/(main)/`.
- [ ] Clean up repository: delete README.md, CONTRIBUTING.md, media/.
- [ ] Delete `src/app/(auth)/` and delete `src/middleware.ts` only after verification.
- [ ] Run `npm run lint` and `npm run build` (or `npm run dev`) to ensure no broken imports.

